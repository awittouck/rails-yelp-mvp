<h1>Full listing restaurants</h1>

<ul>
  <% @restaurants.each do |r|  %>
  <li> <%= r.name %> -- <%= r.category %> <br> <%= r.address %> -- <%= r.phone_number %> </li>
  <% end %>
</ul>

<%= link_to "Add a new restaurant", new_restaurant_path %>
